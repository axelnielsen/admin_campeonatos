<%= form_with(model: referee_level, local: true) do |form| %>
  <% if referee_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(referee_level.errors.count, "error") %> prohibited this referee_level from being saved:</h2>

      <ul>
      <% referee_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :referee_level_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
